<?php
/**
 * Theme hooks.
 * @author  	 DeoThemes
 * @copyright  (c) Copyright by DeoThemes
 * @link       https://deothemes.com
 * @package 	 Dinerize
 * @since 		 1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit( 'Direct script access denied.' );
}

/**
 * Head top
 */
function dinerize_head_top() {
	do_action( 'dinerize_head_top' );
}

/**
 * Head bottom
 */
function dinerize_head_bottom() {
	do_action( 'dinerize_head_bottom' );
}

/**
 * Header before
 */
function dinerize_header_before() {
	do_action( 'dinerize_header_before' );
}

/**
 * Site Header
 */
function dinerize_header() {
	do_action( 'dinerize_header' );
}

/**
 * Header after
 */
function dinerize_header_after() {
	do_action( 'dinerize_header_after' );
}

/**
 * Masthead
 */
function dinerize_masthead() {
	do_action( 'dinerize_masthead' );
}

/**
 * Body top
 */
function dinerize_body_top() {
	do_action( 'dinerize_body_top' );
}

/**
 * Body bottom
 */
function dinerize_body_bottom() {
	do_action( 'dinerize_body_bottom' );
}

/**
 * Primary content top
 */
function dinerize_primary_content_top() {
	do_action( 'dinerize_primary_content_top' );
}

/**
 * Primary content before
 */
function dinerize_primary_content_before() {
	do_action( 'dinerize_primary_content_before' );
}

/**
 * Primary content query
 */
function dinerize_primary_content_query() {
	do_action( 'dinerize_primary_content_query' );
}

/**
 * Primary content after
 */
function dinerize_primary_content_after() {
	do_action( 'dinerize_primary_content_after' );
}

/**
 * Primary content bottom
 */
function dinerize_primary_content_bottom() {
	do_action( 'dinerize_primary_content_bottom' );
}

/**
 * Comments before
 */
function dinerize_comments_before() {
	do_action( 'dinerize_comments_before' );
}


/**
 * Comments
 */
function dinerize_comments() {
	do_action( 'dinerize_comments' );
}

/**
 * Comments after
 */
function dinerize_comments_after() {
	do_action( 'dinerize_comments_after' );
}


/**
 * Entry section before
 */
function dinerize_entry_section_before() {
	do_action( 'dinerize_entry_section_before' );
}

/**
 * Entry header before
 */
function dinerize_entry_header_before() {
	do_action( 'dinerize_entry_header_before' );
}

/**
 * Entry header
 */
function dinerize_entry_header() {
	do_action( 'dinerize_entry_header' );
}

/**
 * Entry header after
 */
function dinerize_entry_header_after() {
	do_action( 'dinerize_entry_header_after' );
}

/**
 * Entry featured image
 */
function dinerize_entry_featured_image() {
	do_action( 'dinerize_entry_featured_image' );
}

/**
 * Entry content top
 */
function dinerize_entry_content_top() {
	do_action( 'dinerize_entry_content_top' );
}

/**
 * Entry content bottom
 */
function dinerize_entry_content_bottom() {
	do_action( 'dinerize_entry_content_bottom' );
}

/**
 * Page title before
 */
function dinerize_page_title_before() {
	do_action( 'dinerize_page_title_before' );
}

/**
 * Page title after
 */
function dinerize_page_title_after() {
	do_action( 'dinerize_page_title_after' );
}


/**
 * Sidebar before
 */
function dinerize_sidebar_before() {
	do_action( 'dinerize_sidebar_before' );
}

/**
 * Sidebar after
 */
function dinerize_sidebar_after() {
	do_action( 'dinerize_sidebar_after' );
}

/**
 * Footer before
 */
function dinerize_footer_before() {
	do_action( 'dinerize_footer_before' );
}

/**
 * Footer
 */
function dinerize_footer() {
	do_action( 'dinerize_footer' );
}

/**
 * Footer after
 */
function dinerize_footer_after() {
	do_action( 'dinerize_footer_after' );
}